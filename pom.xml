<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.llamaquill</groupId>
  <artifactId>LlamaQuill</artifactId>
  <version>0.1.0</version>
  <name>LlamaQuill</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <javafx.version>25.0.1</javafx.version>
    <javafx.platform>win</javafx.platform>
    <javafx.maven.plugin.version>0.0.8</javafx.maven.plugin.version>

    <junit.jupiter.version>5.10.3</junit.jupiter.version>
    <jpackage.plugin.version>1.7.3</jpackage.plugin.version>

    <app.name>LlamaQuill</app.name>
    <app.vendor>com.llamaquill</app.vendor>
    <app.module>com.llamaquill</app.module>
    <app.mainClass>com.llamaquill.App</app.mainClass>
    <app.description>LlamaQuill desktop app</app.description>

    <staging.dir>${project.build.directory}/mods</staging.dir>
    <dist.dir>${project.build.directory}/dist</dist.dir>
    <release.package.type>APP_IMAGE</release.package.type>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-base</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-graphics</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>

    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.46.1.0</version>
    </dependency>

    <dependency>
      <groupId>org.json</groupId>
      <artifactId>json</artifactId>
      <version>20240303</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.jupiter.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>${javafx.maven.plugin.version}</version>
        <configuration>
          <mainClass>${app.module}/${app.mainClass}</mainClass>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jar-plugin</artifactId>
            <version>3.4.2</version>
            <configuration>
              <outputDirectory>${staging.dir}</outputDirectory>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <version>3.7.1</version>
            <executions>
              <execution>
                <id>copy-runtime-dependencies</id>
                <phase>package</phase>
                <goals>
                  <goal>copy-dependencies</goal>
                </goals>
                <configuration>
                  <includeScope>runtime</includeScope>
                  <outputDirectory>${staging.dir}</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>clean-jpackage-output</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <target>
                    <mkdir dir="${dist.dir}"/>
                    <attrib readonly="false" failonerror="false">
                      <fileset dir="${dist.dir}" includes="**/*"/>
                    </attrib>
                    <delete dir="${dist.dir}" includeemptydirs="true" failonerror="false" quiet="true"/>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.panteleyev</groupId>
            <artifactId>jpackage-maven-plugin</artifactId>
            <version>${jpackage.plugin.version}</version>
            <executions>
              <execution>
                <id>package-release-image</id>
                <phase>package</phase>
                <goals>
                  <goal>jpackage</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <name>${app.name}</name>
              <vendor>${app.vendor}</vendor>
              <description>${app.description}</description>
              <appVersion>${project.version}</appVersion>
              <input>${staging.dir}</input>
              <mainJar>${project.build.finalName}.jar</mainJar>
              <mainClass>${app.mainClass}</mainClass>
              <modulePaths>
                <modulePath>${staging.dir}</modulePath>
              </modulePaths>
              <addModules>
                <module>javafx.base</module>
                <module>javafx.controls</module>
                <module>javafx.graphics</module>
                <module>java.net.http</module>
                <module>java.sql</module>
                <module>jdk.crypto.ec</module>
              </addModules>
              <destination>${dist.dir}</destination>
              <removeDestination>true</removeDestination>
              <type>${release.package.type}</type>
              <jLinkOptions>
                <option>--strip-debug</option>
                <option>--no-header-files</option>
                <option>--no-man-pages</option>
              </jLinkOptions>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
